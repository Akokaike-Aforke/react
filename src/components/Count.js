import ClickHandler from "./ClickHandler";
function Count(props)
{
    return(
        <div className="zeroDiv">
            <h1 className="zero">{props.number}</h1>
        </div>
    )
}
export default Count;